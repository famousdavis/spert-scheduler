{
  "repo": "spert-scheduler",

  "versionConstant": {
    "file": "src/app/constants.ts",
    "export": "APP_VERSION"
  },

  "changelog": {
    "file": "CHANGELOG.md",
    "headingPattern": "^## (\\d+\\.\\d+\\.\\d+)\\s*—",

    "_extraSurfacesNote": "The served copy here had gone FIVE MONTHS stale before it was caught, because nothing read it: no build, test or check touched public/CHANGELOG.md. That is the specific incident this check exists to prevent recurring.",

    "extraSurfaces": [
      {
        "file": "public/CHANGELOG.md",
        "match": "identical",
        "note": "Served copy. Keep byte-identical — copy the root file, do not hand-edit both."
      },
      {
        "file": "src/ui/pages/changelog-data.ts",
        "match": "firstVersion",
        "note": "In-app changelog. Uses double quotes throughout; the default pattern accepts either."
      }
    ]
  },

  "claudeMdVersionPatterns": [],

  "_claudeMdNote": "Deliberately empty. Swept on 2026-07-30: CLAUDE.md contains 21 lines carrying an X.Y.Z string, and every one is historical prose in the Key Files table — '(v0.59.0)', '(v0.49.0)', 're-synced v0.57.4'. Those are correct as written and must never be flagged. There is no `Current Version` / `Live facts` style declaration line to check. If one is ever added, add its pattern here. (MyScrumBudget shipped a guard that matched only one of its two declarations and missed a stale second one — sweep the whole file for X.Y.Z before assuming, and note that `\\b\\d` will not match `v0.59.0`.) The '1719 tests across 83 test files' claim in Testing Patterns is a COUNT, not a version, so it cannot be machine-checked against package.json — update it by hand.",

  "gitBaseBranch": "main",

  "_gitBaseBranchNote": "Consumed by TWO scripts at two different moments, and the split is the point. shipgate.mjs uses it PRE-merge for one assertion only — that this clone is not BEHIND origin/<branch>, i.e. the release is being cut on top of current main. verify-sync.mjs uses it POST-merge for the three-way SHA comparison (local, remote-tracking, GitHub API) plus a clean-tree check. Neither check can live in the other's moment: the gate runs partway through a release with the version and changelog edits UNCOMMITTED, so a clean-tree assertion there would fail every release; and the post-merge SHA condition does not exist yet when the gate runs. Both self-skip under CI: actions/checkout is shallow and detached, so origin/<branch> is not a meaningful local ref there, and a CI runner has no view of a developer's clone at all — which is why this drift was invisible to every mechanism the suite already had. Remove this key to disable both checks; the gate then prints nothing for them rather than passing silently.",

  "commands": [
    {
      "name": "lint",
      "run": "npm run lint",
      "expectProblems": 3,
      "_note": "eslint exits NON-ZERO here and that is correct — gate on the NUMBER not moving, never on the exit code. `expectProblems` above is the single source of truth for the accepted baseline; it is deliberately the only place the count is written, so there is no second copy to go stale. Re-derive it from `npm run lint` immediately before each commit and set it in the same commit. The baseline is mostly sonarjs/cognitive-complexity on core scheduling and simulation functions, plus react-hooks/set-state-in-effect. William confirmed it as accepted; do NOT refactor those functions merely to satisfy the threshold. The ratchet fails in BOTH directions — if problems were resolved, lower it."
    },
    { "name": "test", "run": "npm test" },
    {
      "name": "build (tsc -b && vite build)",
      "run": "npm run build",
      "_note": "`tsc -b` covers test files here — tsconfig.app.json includes src/**/*.ts, and injecting an error into a .test.ts was verified to fail the build. So no separate typecheck step is needed, unlike MyScrumBudget where `next build` skipped tests entirely."
    }
  ],

  "_contractHashNote": "`npm run contract:hash` is deliberately NOT a gate step. It PRINTS the digest and exits 0 whatever it finds, so as a command it would be a guard-shaped no-op — it only ever helped when a human ran it in both repos and compared by eye. The real assertion now lives in src/integration/ai-op-contract-hash.test.ts, which pins the digest and therefore runs in the always-true tier. The script stays for the two-repo comparison workflow."
}
