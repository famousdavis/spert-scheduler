# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test
coverage/

# Vite
*.local

# Vite drops these at the project root when it bundles a TypeScript config, and
# leaves them behind if the process is killed. They sit beside vite.config.ts
# with a .mjs extension, which src/integration/copyright-headers.test.ts scopes
# as a root config file — an abandoned one turns `npm test` red for a reason the
# failure message cannot explain.
vite.config.*.timestamp-*
vitest.config.*.timestamp-*

# Claude Code
CLAUDE.md
.claude/

# Stryker mutation testing output
reports/mutation/
.stryker-tmp/

# Vercel
.vercel

lint-baseline.json
